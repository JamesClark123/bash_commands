#!/usr/bin/env bash
# This script was generated by bashly 1.4.0 (https://bashly.dev)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if ((BASH_VERSINFO[0] < 4 || (BASH_VERSINFO[0] == 4 && BASH_VERSINFO[1] < 2))); then
  printf "bash version 4.2 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
git_cli_usage() {
  printf "git-cli - Custom cli wrapper for git\n\n"

  printf "%s\n" "Usage:"
  printf "  git-cli COMMAND\n"
  printf "  git-cli [COMMAND] --help | -h\n"
  printf "  git-cli --version | -v\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Show the commit history.\n" "log                "
  printf "  %s   Show the working tree status.\n" "status             "
  printf "  %s   Show changes between commits, branches, the index and the working tree.\n" "diff               "
  printf "  %s   Switch branches or restore working tree files.\n" "checkout           "
  printf "  %s   Stage file contents for the next commit.\n" "add                "
  printf "  %s   Record staged changes to the repository.\n" "commit             "
  printf "  %s   Update remote refs along with associated objects.\n" "push               "
  printf "  %s   Fetch from and integrate with another repository or local branch.\n" "pull               "
  printf "  %s   Download objects and refs from another repository.\n" "fetch              "
  printf "  %s   Reapply commits on top of another base.\n" "rebase             "
  printf "  %s   Reset the current HEAD to a specified state.\n" "reset              "
  printf "  %s   Restore working tree files.\n" "restore            "
  printf "  %s   Apply the changes introduced by existing commits.\n" "cherry-pick        "
  printf "  %s   Create, list, or delete tags.\n" "tag                "
  printf "  %s   Prune and delete merged local branches.\n" "clean              "
  printf "  %s   Update a branch with the latest changes from the remote.\n" "update             "
  printf "  %s   Commit, then restack every branch built on top of the replaced commit.\n" "commit-update-stack"
  printf "  %s   Force push the current branch and every branch stacked downstream of it.\n" "push-force-stack   "
  printf "  %s   Move up the stack, away from main, to a child of the current commit.\n" "stack-up           "
  printf "  %s   Move down the stack, toward main, to a parent of the current commit.\n" "stack-down         "
  printf "  %s   Pick a downstream branch of the current commit and check it out.\n" "stack-up-select    "
  printf "  %s   Pick an upstream branch of the current commit and check it out.\n" "stack-down-select  "
  printf "  %s   Generate shell completion definitions for the git-cli tool.\n" "completions        "
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "--version, -v"
    printf "    Show version number\n"
    echo

  fi
}

# :command.usage
git_cli_log_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli log\n\n"
    printf "  Show the commit history.\n  Passes through to \`git log\` and accepts any of its flags/args.\n  Use -g for a decorated, one-line graph view of the history.\n\n"
  else
    printf "git-cli log - Show the commit history.\n\n"
  fi
  printf "Alias: l\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli log [OPTIONS] [--] [...]\n"
  printf "  git-cli log --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--graph, -g"
    printf "    Show history as a one-line decorated commit graph\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g l                     # recent commits (shell alias: gl)\n"
    printf "  g l -g                  # one-line graph view (shell alias: glg)\n"
    printf "  g l --oneline -10       # last 10 commits, condensed\n"
    printf "  g l main..HEAD          # commits on this branch not yet on main\n"
    echo

  fi
}

# :command.usage
git_cli_status_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli status\n\n"
    printf "  Show the working tree status.\n  Passes through to \`git status\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli status - Show the working tree status.\n\n"
  fi
  printf "Alias: s\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli status [--] [...]\n"
  printf "  git-cli status --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g s                     # working tree status (shell alias: gs)\n"
    printf "  g s -s                  # short-format status\n"
    printf "  g s -b                  # include branch and tracking info\n"
    echo

  fi
}

# :command.usage
git_cli_diff_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli diff\n\n"
    printf "  Show changes between commits, branches, the index and the working tree.\n  Passes through to \`git diff\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli diff - Show changes between commits, branches, the index and the working tree.\n\n"
  fi
  printf "Alias: d\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli diff [--] [...]\n"
  printf "  git-cli diff --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g d                     # unstaged changes only\n"
    printf "  g d --staged            # staged changes (what would be committed)\n"
    printf "  g d --stat              # summary of changed files (shell alias: gds)\n"
    printf "  g d branch1..branch2    # difference between two branches\n"
    printf "  g d HEAD~3              # changes since 3 commits ago\n"
    echo

  fi
}

# :command.usage
git_cli_checkout_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli checkout\n\n"
    printf "  Switch branches or restore working tree files.\n  Passes through to \`git checkout\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli checkout - Switch branches or restore working tree files.\n\n"
  fi
  printf "Alias: co\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli checkout [--] [...]\n"
  printf "  git-cli checkout --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g co main               # switch to the main branch (shell alias: gco)\n"
    printf "  g co -                  # switch back to the previous branch\n"
    printf "  g co -b feature/login   # create and switch to a new branch\n"
    printf "  g co -B feature/login   # create or reset a branch, then switch (shell alias:\n  gcoB)\n"
    echo

  fi
}

# :command.usage
git_cli_add_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli add\n\n"
    printf "  Stage file contents for the next commit.\n  Passes through to \`git add\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli add - Stage file contents for the next commit.\n\n"
  fi
  printf "Alias: a\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli add [--] [...]\n"
  printf "  git-cli add --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g a .                   # stage everything in the current directory\n"
    printf "  g a -u                  # stage modified/deleted tracked files (shell alias:\n  gau)\n"
    printf "  g a -A                  # stage all changes, including new files (shell alias:\n  gaA)\n"
    printf "  g a -p                  # interactively stage hunks\n"
    echo

  fi
}

# :command.usage
git_cli_commit_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli commit\n\n"
    printf "  Record staged changes to the repository.\n  With no arguments, opens an interactive picker (Amend, Amend no edit,\n  Fixup, Squash) to build the commit for you. Otherwise passes through to\n  \`git commit\` with any flags/args you supply.\n\n"
  else
    printf "git-cli commit - Record staged changes to the repository.\n\n"
  fi
  printf "Alias: c\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli commit [--] [...]\n"
  printf "  git-cli commit --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g c                     # interactive commit-type picker\n"
    printf "  g c -m \"fix: typo\"      # commit staged changes with a message\n"
    printf "  g c --amend             # amend the previous commit (shell alias: gca)\n"
    printf "  g c --amend --no-edit   # amend without editing the message (shell alias:\n  gcan)\n"
    echo

  fi
}

# :command.usage
git_cli_push_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli push\n\n"
    printf "  Update remote refs along with associated objects.\n  Passes through to \`git push\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli push - Update remote refs along with associated objects.\n\n"
  fi
  printf "Alias: ph\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli push [--] [...]\n"
  printf "  git-cli push --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g ph                    # push current branch (shell alias: gph)\n"
    printf "  g ph -f                 # force push (shell alias: gpf)\n"
    printf "  g ph origin --tags      # push all tags (shell alias: gpt)\n"
    printf "  g ph --set-upstream origin <branch>   # push and set upstream (shell alias:\n  gpsu)\n"
    echo

  fi
}

# :command.usage
git_cli_pull_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli pull\n\n"
    printf "  Fetch from and integrate with another repository or local branch.\n  Passes through to \`git pull\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli pull - Fetch from and integrate with another repository or local branch.\n\n"
  fi
  printf "Alias: pl\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli pull [--] [...]\n"
  printf "  git-cli pull --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g pl                    # pull the current branch (shell alias: gpl)\n"
    printf "  g pl --rebase           # pull and rebase local commits on top\n"
    printf "  g pl origin main        # pull a specific remote branch\n"
    echo

  fi
}

# :command.usage
git_cli_fetch_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli fetch\n\n"
    printf "  Download objects and refs from another repository.\n  Passes through to \`git fetch\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli fetch - Download objects and refs from another repository.\n\n"
  fi
  printf "Alias: f\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli fetch [--] [...]\n"
  printf "  git-cli fetch --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g f                     # fetch from the default remote\n"
    printf "  g f --prune             # fetch and remove stale remote refs (shell alias:\n  gfp)\n"
    printf "  g f --all               # fetch from all remotes\n"
    echo

  fi
}

# :command.usage
git_cli_rebase_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli rebase\n\n"
    printf "  Reapply commits on top of another base.\n  With no arguments, walks you through an interactive flow: pick a target\n  branch, then a base (merge-base, HEAD~, or a commit chosen from the log)\n  and rebases the current branch onto it with --autosquash. Otherwise passes\n  through to \`git rebase\` with any flags/args you supply.\n\n"
  else
    printf "git-cli rebase - Reapply commits on top of another base.\n\n"
  fi
  printf "Alias: r\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli rebase [--] [...]\n"
  printf "  git-cli rebase --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g r                     # interactive guided rebase (shell alias: gr)\n"
    printf "  g r --continue          # continue after resolving conflicts (shell alias:\n  grc)\n"
    printf "  g r --abort             # cancel an in-progress rebase\n"
    printf "  g r --autosquash -i --onto main HEAD~1 <branch>   # squash onto main (shell\n  alias: grm)\n"
    echo

  fi
}

# :command.usage
git_cli_reset_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli reset\n\n"
    printf "  Reset the current HEAD to a specified state.\n  Passes through to \`git reset\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli reset - Reset the current HEAD to a specified state.\n\n"
  fi
  printf "Alias: rt\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli reset [--] [...]\n"
  printf "  git-cli reset --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g rt HEAD~1             # undo last commit, keep changes in the working tree\n"
    printf "  g rt --soft HEAD~1      # undo last commit, keep changes staged\n"
    printf "  g rt --hard origin/<branch>   # discard local changes to match remote (shell\n  alias: grh)\n"
    echo

  fi
}

# :command.usage
git_cli_restore_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli restore\n\n"
    printf "  Restore working tree files.\n  Passes through to \`git restore\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli restore - Restore working tree files.\n\n"
  fi
  printf "Alias: re\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli restore [--] [...]\n"
  printf "  git-cli restore --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g re file.txt           # discard unstaged changes to a file\n"
    printf "  g re --staged file.txt  # unstage a file, keeping its changes\n"
    printf "  g re \"*\"                # discard all unstaged changes (shell alias: grestA)\n"
    echo

  fi
}

# :command.usage
git_cli_cherry_pick_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli cherry-pick\n\n"
    printf "  Apply the changes introduced by existing commits.\n  Passes through to \`git cherry-pick\` and accepts any of its flags/args.\n\n"
  else
    printf "git-cli cherry-pick - Apply the changes introduced by existing commits.\n\n"
  fi
  printf "Alias: cp\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli cherry-pick [--] [...]\n"
  printf "  git-cli cherry-pick --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g cp <commit>           # apply a commit onto the current branch (shell alias:\n  gcp)\n"
    printf "  g cp <commitA> <commitB>   # cherry-pick multiple commits\n"
    printf "  g cp --continue         # continue after resolving conflicts\n"
    printf "  g cp --abort            # cancel the cherry-pick\n"
    echo

  fi
}

# :command.usage
git_cli_tag_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli tag\n\n"
    printf "  Create, list, or delete tags.\n  Passes through to \`git tag\`. With --sync, pushes all tags to origin\n  after the tag operation completes.\n\n"
  else
    printf "git-cli tag - Create, list, or delete tags.\n\n"
  fi
  printf "Alias: t\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli tag [OPTIONS] [--] [...]\n"
  printf "  git-cli tag --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--sync, -s"
    printf "    Push all tags to origin after creating the tag\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g t                     # list existing tags (shell alias: gt)\n"
    printf "  g t v1.2.0              # create a lightweight tag\n"
    printf "  g t v1.2.0 --sync       # create a tag and push tags to origin (shell alias:\n  gts)\n"
    printf "  g t -d v1.2.0           # delete a local tag\n"
    echo

  fi
}

# :command.usage
git_cli_clean_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli clean\n\n"
    printf "  Prune and delete merged local branches.\n  Fetches with --prune, then finds local branches whose upstream is gone\n  ([gone]) and lets you choose which to delete (select none to delete all).\n\n"
  else
    printf "git-cli clean - Prune and delete merged local branches.\n\n"
  fi

  printf "%s\n" "Usage:"
  printf "  git-cli clean\n"
  printf "  git-cli clean --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g clean                 # prune remotes and delete gone branches (shell alias:\n  gclean)\n"
    echo

  fi
}

# :command.usage
git_cli_update_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli update\n\n"
    printf "  Update a branch with the latest changes from the remote.\n  Checks out the branch (interactive picker if none is given), fetches with\n  --prune, and pulls. Returns to your previous branch afterwards unless\n  --stay is set.\n\n"
  else
    printf "git-cli update - Update a branch with the latest changes from the remote.\n\n"
  fi
  printf "Alias: u\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli update [BRANCH] [OPTIONS]\n"
  printf "  git-cli update --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--stay, -s"
    printf "    Stay on the updated branch instead of returning to the previous one\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "BRANCH"
    printf "    The branch to update (omit to pick interactively)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g update                # pick a branch to update, then return (shell alias:\n  gup)\n"
    printf "  g update main           # update main, then return (shell alias: gupm)\n"
    printf "  g update --stay         # update a chosen branch and stay on it (shell alias:\n  gups)\n"
    printf "  g update main --stay    # update main and stay on it (shell alias: gupms)\n"
    echo

  fi
}

# :command.usage
git_cli_commit_update_stack_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli commit-update-stack\n\n"
    printf "  Commit, then restack every branch built on top of the replaced commit.\n  Accepts and passes through any \`git commit\` flags/args (with no arguments\n  it opens the same interactive picker as \`g commit\`). After committing it\n  finds the terminal branches stacked downstream of the old commit and\n  rebases each onto the new commit with \`git rebase --update-refs\`, keeping\n  intermediate branch refs in sync across every fork of the stack. Returns\n  to your starting branch when done.\n\n"
  else
    printf "git-cli commit-update-stack - Commit, then restack every branch built on top of the replaced commit.\n\n"
  fi
  printf "Alias: cus\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli commit-update-stack [--] [...]\n"
  printf "  git-cli commit-update-stack --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g cus                    # interactive commit-type picker, then restack (shell\n  alias: gcus)\n"
    printf "  g cus --amend --no-edit  # amend in place and restack downstream branches\n"
    printf "  g cus -m \"fix: typo\"     # add a commit and move stacked branches on top of it\n"
    echo

  fi
}

# :command.usage
git_cli_push_force_stack_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli push-force-stack\n\n"
    printf "  Force push the current branch and every branch stacked downstream of it.\n  Walks all branches whose history contains the current commit (following\n  every fork of the stack) and force pushes each one that has a matching\n  origin branch; branches without a remote counterpart are skipped.\n\n"
  else
    printf "git-cli push-force-stack - Force push the current branch and every branch stacked downstream of it.\n\n"
  fi
  printf "Alias: pfs\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli push-force-stack\n"
  printf "  git-cli push-force-stack --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g pfs                    # force push the whole stack from here up (shell\n  alias: gpfs)\n"
    echo

  fi
}

# :command.usage
git_cli_stack_up_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli stack-up\n\n"
    printf "  Move up the stack, away from main, to a child of the current commit.\n  If the commit has multiple children a picker is shown. Lands on the\n  branch pointing at the chosen commit when one exists, otherwise checks\n  out the commit directly (detached HEAD).\n\n"
  else
    printf "git-cli stack-up - Move up the stack, away from main, to a child of the current commit.\n\n"
  fi
  printf "Alias: su\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli stack-up\n"
  printf "  git-cli stack-up --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g su                     # move one commit toward the stack tip (shell alias:\n  gsu)\n"
    echo

  fi
}

# :command.usage
git_cli_stack_down_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli stack-down\n\n"
    printf "  Move down the stack, toward main, to a parent of the current commit.\n  If the commit has multiple parents (a merge) a picker is shown. Lands on\n  the branch pointing at the chosen commit when one exists, otherwise\n  checks out the commit directly (detached HEAD).\n\n"
  else
    printf "git-cli stack-down - Move down the stack, toward main, to a parent of the current commit.\n\n"
  fi
  printf "Alias: sd\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli stack-down\n"
  printf "  git-cli stack-down --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g sd                     # move one commit toward main (shell alias: gsd)\n"
    echo

  fi
}

# :command.usage
git_cli_stack_up_select_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli stack-up-select\n\n"
    printf "  Pick a downstream branch of the current commit and check it out.\n  Lists every branch stacked on top of the current commit (across all forks\n  of the stack, nearest first) in a picker; auto-selects when there is only\n  one candidate.\n\n"
  else
    printf "git-cli stack-up-select - Pick a downstream branch of the current commit and check it out.\n\n"
  fi
  printf "Alias: sus\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli stack-up-select\n"
  printf "  git-cli stack-up-select --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g sus                    # pick any branch above this one (shell alias: gsus)\n"
    echo

  fi
}

# :command.usage
git_cli_stack_down_select_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli stack-down-select\n\n"
    printf "  Pick an upstream branch of the current commit and check it out.\n  Lists every branch below the current commit (nearest first), searching no\n  farther back than the merge base with main; auto-selects when there is\n  only one candidate.\n\n"
  else
    printf "git-cli stack-down-select - Pick an upstream branch of the current commit and check it out.\n\n"
  fi
  printf "Alias: sds\n"
  echo

  printf "%s\n" "Usage:"
  printf "  git-cli stack-down-select\n"
  printf "  git-cli stack-down-select --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  g sds                    # pick any branch below this one (shell alias: gsds)\n"
    echo

  fi
}

# :command.usage
git_cli_completions_usage() {
  if [[ -n $long_usage ]]; then
    printf "git-cli completions\n\n"
    printf "  Generate shell completion definitions for the git-cli tool.\n  Intended to be evaluated from your shell profile, e.g. eval \"$(g\n  completions)\".\n\n"
  else
    printf "git-cli completions - Generate shell completion definitions for the git-cli tool.\n\n"
  fi

  printf "%s\n" "Usage:"
  printf "  git-cli completions\n"
  printf "  git-cli completions --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    # :command.usage_options
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  eval \"$(g completions)\"   # load completions in the current shell\n"
    echo

  fi
}

# :command.normalize_input
# :command.normalize_input_function
normalize_input() {
  local arg passthru flags
  passthru=false

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $passthru == true ]]; then
      input+=("$arg")
    elif [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    elif [[ "$arg" == "--" ]]; then
      passthru=true
      input+=("$arg")
    else
      input+=("$arg")
    fi

    shift
  done
}

# :command.inspect_args
inspect_args() {
  local k

  if ((${#args[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
    echo args:
    for k in "${sorted_keys[@]}"; do
      echo "- \${args[$k]} = ${args[$k]}"
    done
  else
    echo args: none
  fi

  if ((${#other_args[@]})); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for k in "${!other_args[@]}"; do
      echo "- \${other_args[$k]} = ${other_args[$k]}"
    done
  fi

  if ((${#deps[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
    echo
    echo deps:
    for k in "${sorted_keys[@]}"; do
      echo "- \${deps[$k]} = ${deps[$k]}"
    done
  fi

  if ((${#env_var_names[@]})); then
    readarray -t sorted_names < <(printf '%s\n' "${env_var_names[@]}" | sort)
    echo
    echo "environment variables:"
    for k in "${sorted_names[@]}"; do
      echo "- \$$k = ${!k:-}"
    done
  fi
}

# :command.user_lib
# src/lib/branch_hash.sh
# Returns the full hash for the given branch name.
branch_hash () {
    git show-ref --verify --hash refs/heads/$1
}

# src/lib/checkout_branch_or_ref.sh
# Checks out the local branch pointing at the given ref when one exists,
# otherwise checks out the ref directly (detached HEAD).
checkout_branch_or_ref () {
    local branch
    branch=$(get_branches_pointing_at_ref "$1" | head -n 1)

    if [[ -n "$branch" ]]; then
        git checkout "$branch"
    else
        git checkout "$1"
    fi
}

# src/lib/choose_branch.sh
# Prompts the user to choose one of the given branch names, each shown as
# "<branch> <short-hash> <subject>". Auto-selects when only one is given.
# Usage: choose_branch <header> <branch>...
# Echoes the chosen branch name; returns 1 if the prompt is cancelled.
choose_branch () {
    local header=$1
    shift

    if [[ $# -eq 1 ]]; then
        echo "$1"
        return 0
    fi

    local branch chosen options=()
    for branch in "$@"; do
        options+=("$branch $(git log -1 --format='%h %s' "$branch")")
    done

    chosen=$(gum choose "${options[@]}" --header "$header" --limit 1) || return 1
    [[ -z "$chosen" ]] && return 1

    echo "$chosen" | awk '{print $1}'
}

# src/lib/choose_commit.sh
# Prompts the user to choose one of the given commit hashes, each shown as
# "<short-hash> <subject> [branches]". Auto-selects when only one is given.
# Usage: choose_commit <header> <hash>...
# Echoes the chosen full hash; returns 1 if the prompt is cancelled.
choose_commit () {
    local header=$1
    shift

    if [[ $# -eq 1 ]]; then
        git rev-parse "$1"
        return 0
    fi

    local hash line branches chosen options=()
    for hash in "$@"; do
        line=$(git log -1 --format='%h %s' "$hash")
        branches=$(get_branches_pointing_at_ref "$hash" | paste -s -d ',' -)
        if [[ -n "$branches" ]]; then
            line="$line [$branches]"
        fi
        options+=("$line")
    done

    chosen=$(gum choose "${options[@]}" --header "$header" --limit 1) || return 1
    [[ -z "$chosen" ]] && return 1

    git rev-parse "$(echo "$chosen" | awk '{print $1}')"
}

# src/lib/contains.sh
# Returns 0 if $2 is a substring of $1, else 1.
contains () {
    local string="$1"
    local substring="$2"

    if [ "${string#*"$substring"}" != "$string" ]; then
        return 0
    else
        return 1
    fi
}

# src/lib/current_branch_hash.sh
# Returns the full hash for the currently checked-out branch.
current_branch_hash () {
    branch_hash $(current_branch_name)
}

# src/lib/current_branch_name.sh
# Returns the name of the currently checked-out branch.
current_branch_name () {
    git branch --show-current | tail -n 1
}

# src/lib/current_hash.sh
# Returns the full hash of HEAD.
current_hash () {
    git show-ref --hash --head --verify HEAD
}

# src/lib/current_hash_has_branch.sh
# Returns 0 if the current HEAD hash has an associated local branch.
current_hash_has_branch () {
    currHash=$(current_hash)
    currBranch=$(get_branch_for_ref $currHash)
    if contains $currBranch "HEAD detached at"
    then
        return 1
    else
        return 0
    fi
}

# src/lib/current_hash_has_remote_branch.sh
# Returns 0 if the current HEAD hash has a corresponding remote branch on origin.
current_hash_has_remote_branch () {
    currBranch=$(get_branch_for_ref $(current_hash))
    git show-ref --verify --quiet refs/remotes/origin/$currBranch
}

# src/lib/first_base_commit_off_main.sh
# Returns the hash of the first commit on the current branch that diverged from main.
first_base_commit_off_main () {
    git log main..HEAD --pretty=format:"%h" | tail -1
}

# src/lib/get_all_local_branches.sh

get_all_local_branches() {
    git branch --format='%(refname:short)'
}

# src/lib/get_branch_for_ref.sh
# Returns the single most relevant branch name for a given ref,
# excluding child branches when possible.
get_branch_for_ref () {
    ref=$(get_parent_child_hash $1)
    if (has_child $ref)
    then
        childHash=$(get_child_hash $ref)
        git branch --contains $1 --no-contains $childHash | tail -n 1 | sed 's/*/ /'  | xargs
    else
        git branch --contains $1 | tail -n 1 | sed 's/*/ /'  | xargs
    fi
}

# src/lib/get_branches_for_ref.sh
# Returns all branch names that contain the given ref.
get_branches_for_ref () {
    git branch --contains $1 | tail -n 1 | sed 's/*/ /' | xargs
}

# src/lib/get_branches_pointing_at_ref.sh
# Returns the local branch names whose tip is exactly the given ref.
get_branches_pointing_at_ref () {
    git for-each-ref refs/heads --points-at "$1" --format='%(refname:short)'
}

# src/lib/get_child_hash.sh
# Extracts the first child hash (second token) from a "<parent> <child...>"
# hash string. Joins all args with spaces so unquoted call sites still work.
get_child_hash () {
    echo "$*" | cut -d " " -f 2
}

# src/lib/get_child_hashes_for_ref.sh
# Returns the direct child commit hashes of the given ref that are reachable
# from local branches (one full hash per line).
get_child_hashes_for_ref () {
    git rev-list --children --branches | awk -v ref="$(git rev-parse "$1")" '$1 == ref { for (i = 2; i <= NF; i++) print $i }'
}

# src/lib/get_current_child_hash.sh
# Returns the child hash of the current HEAD's parent-child pair.
get_current_child_hash () {
    ret="$(get_current_parent_child_hash)"
    get_child_hash $ret
}

# src/lib/get_current_parent_child_hash.sh
# Same as get_parent_child_hash but for the current branch's tip.
get_current_parent_child_hash () {
    get_parent_child_hash $(current_branch_hash)
}

# src/lib/get_downstream_branches_for_ref.sh
# Returns the local branches stacked downstream of the given ref — branches
# whose tip contains the ref but is not the ref itself — nearest first,
# excluding main and master (one per line).
get_downstream_branches_for_ref () {
    local refHash branch tip
    refHash=$(git rev-parse "$1")

    for branch in $(get_stack_branches_for_ref "$refHash"); do
        tip=$(branch_hash "$branch")
        [[ "$tip" == "$refHash" ]] && continue
        echo "$(git rev-list --count "$tip") $branch"
    done | sort -n | cut -d ' ' -f 2
}

# src/lib/get_last_grand_child_hash.sh
# Recursively walks the commit chain from $1, returning the last descendant hash.
get_last_grand_child_hash () {
    ref=$1
    parentChildHash=$(get_parent_child_hash $1)
    if (has_child $parentChildHash);
    then
        newRef=$(get_child_hash $parentChildHash)
        echo $(get_last_grand_child_hash $newRef)
    else
        echo $1
    fi
}

# src/lib/get_last_grand_child_hash_from_current.sh
# Same as get_last_grand_child_hash but starts from the current branch tip.
get_last_grand_child_hash_from_current () {
    get_last_grand_child_hash $(current_branch_hash)
}

# src/lib/get_parent_child_hash.sh
# Given a commit hash, returns a "<hash> <child-hash>" string if a child exists.
get_parent_child_hash () {
    git rev-list --children --all | grep ^$1
}

# src/lib/get_stack_branches_for_ref.sh
# Returns the local branches whose history contains the given ref, excluding
# main and master (one per line).
get_stack_branches_for_ref () {
    git branch --contains "$1" --format='%(refname:short)' | grep -v '^(' | grep -v -x -e 'main' -e 'master'
}

# src/lib/get_terminal_branches_for_ref.sh
# Returns the terminal (leaf) branches of the stack containing the given ref:
# branches that contain the ref and have no other branch stacked on top.
get_terminal_branches_for_ref () {
    local branch

    for branch in $(get_stack_branches_for_ref "$1"); do
        if is_terminal_branch "$branch"; then
            echo "$branch"
        fi
    done
}

# src/lib/get_upstream_branches_for_ref.sh
# Returns the local branches upstream of the given ref — branches whose tip is
# a strict ancestor of the ref — nearest first, searching no farther than the
# merge base with main (or master), inclusive. Excludes main and master
# themselves (one per line).
get_upstream_branches_for_ref () {
    local refHash mainBranch mergeBase branch tip
    refHash=$(git rev-parse "$1")

    mainBranch=main
    git show-ref --verify --quiet refs/heads/main || mainBranch=master
    git show-ref --verify --quiet "refs/heads/$mainBranch" || return 1

    mergeBase=$(git merge-base "$refHash" "$mainBranch") || return 1

    for branch in $(git branch --format='%(refname:short)' | grep -v '^(' | grep -v -x -e 'main' -e 'master'); do
        tip=$(branch_hash "$branch")
        [[ "$tip" == "$refHash" ]] && continue
        git merge-base --is-ancestor "$tip" "$refHash" || continue
        git merge-base --is-ancestor "$mergeBase" "$tip" || continue
        echo "$(git rev-list --count "$tip") $branch"
    done | sort -rn | cut -d ' ' -f 2
}

# src/lib/has_child.sh
# Returns 0 if the given "<parent> <child...>" hash string has a child hash.
# Joins all args with spaces so unquoted call sites still work.
has_child () {
    [[ "$*" == *" "* ]]
}

# src/lib/is_terminal_branch.sh
# Returns 0 if the given branch is a terminal (leaf) branch of its stack,
# i.e. no other local branch's tip is a descendant of its tip.
is_terminal_branch () {
    local tip other
    tip=$(branch_hash "$1")

    for other in $(git branch --contains "$tip" --format='%(refname:short)' | grep -v '^('); do
        [[ "$other" == "$1" ]] && continue
        [[ "$(branch_hash "$other")" != "$tip" ]] && return 1
    done

    return 0
}

# src/lib/send_completions.sh
send_completions() {
  echo $'# git-cli completion                                       -*- shell-script -*-'
  echo $''
  echo $'# This bash completions script was generated by'
  echo $'# completely (https://github.com/bashly-framework/completely)'
  echo $'# Modifying it manually is not recommended'
  echo $''
  echo $'_git-cli_completions_node_flag_state() {'
  echo $'  case "$1:$2" in'
  echo $'    0:--help|0:-h) return 0 ;;'
  echo $'    0:--version|0:-v) return 0 ;;'
  echo $'    1:--help|1:-h) return 0 ;;'
  echo $'    1:--graph|1:-g) return 0 ;;'
  echo $'    2:--help|2:-h) return 0 ;;'
  echo $'    3:--help|3:-h) return 0 ;;'
  echo $'    4:--help|4:-h) return 0 ;;'
  echo $'    5:--help|5:-h) return 0 ;;'
  echo $'    6:--help|6:-h) return 0 ;;'
  echo $'    7:--help|7:-h) return 0 ;;'
  echo $'    8:--help|8:-h) return 0 ;;'
  echo $'    9:--help|9:-h) return 0 ;;'
  echo $'    10:--help|10:-h) return 0 ;;'
  echo $'    11:--help|11:-h) return 0 ;;'
  echo $'    12:--help|12:-h) return 0 ;;'
  echo $'    13:--help|13:-h) return 0 ;;'
  echo $'    14:--help|14:-h) return 0 ;;'
  echo $'    14:--sync|14:-s) return 0 ;;'
  echo $'    15:--help|15:-h) return 0 ;;'
  echo $'    16:--help|16:-h) return 0 ;;'
  echo $'    16:--stay|16:-s) return 0 ;;'
  echo $'    17:--help|17:-h) return 0 ;;'
  echo $'    18:--help|18:-h) return 0 ;;'
  echo $'    19:--help|19:-h) return 0 ;;'
  echo $'    20:--help|20:-h) return 0 ;;'
  echo $'    21:--help|21:-h) return 0 ;;'
  echo $'    22:--help|22:-h) return 0 ;;'
  echo $'    23:--help|23:-h) return 0 ;;'
  echo $'  esac'
  echo $''
  echo $'  return 1'
  echo $'}'
  echo $''
  echo $'_git-cli_completions_option_seen() {'
  echo $'  local completed_option option_name'
  echo $'  for completed_option in "${completed_options[@]}"; do'
  echo $'    for option_name in "$@"; do'
  echo $'      [[ "$completed_option" == "$option_name" ]] && return 0'
  echo $'    done'
  echo $'  done'
  echo $''
  echo $'  return 1'
  echo $'}'
  echo $''
  echo $'_git-cli_completions_resolve_node() {'
  echo $'  node_id=0'
  echo $'  node_word_count=0'
  echo $'  positional_index=0'
  echo $''
  echo $'  local word'
  echo $'  for word in "${non_options[@]}"; do'
  echo $'    case "$node_id:$word" in'
  echo $'      0:log)'
  echo $'        node_id=1'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:l)'
  echo $'        node_id=1'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:status)'
  echo $'        node_id=2'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:s)'
  echo $'        node_id=2'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:diff)'
  echo $'        node_id=3'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:d)'
  echo $'        node_id=3'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:checkout)'
  echo $'        node_id=4'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:co)'
  echo $'        node_id=4'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:add)'
  echo $'        node_id=5'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:a)'
  echo $'        node_id=5'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:commit)'
  echo $'        node_id=6'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:c)'
  echo $'        node_id=6'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:push)'
  echo $'        node_id=7'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:ph)'
  echo $'        node_id=7'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:pull)'
  echo $'        node_id=8'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:pl)'
  echo $'        node_id=8'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:fetch)'
  echo $'        node_id=9'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:f)'
  echo $'        node_id=9'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:rebase)'
  echo $'        node_id=10'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:r)'
  echo $'        node_id=10'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:reset)'
  echo $'        node_id=11'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:rt)'
  echo $'        node_id=11'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:restore)'
  echo $'        node_id=12'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:re)'
  echo $'        node_id=12'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:cherry-pick)'
  echo $'        node_id=13'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:cp)'
  echo $'        node_id=13'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:tag)'
  echo $'        node_id=14'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:t)'
  echo $'        node_id=14'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:clean)'
  echo $'        node_id=15'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:update)'
  echo $'        node_id=16'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:u)'
  echo $'        node_id=16'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:commit-update-stack)'
  echo $'        node_id=17'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:cus)'
  echo $'        node_id=17'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:push-force-stack)'
  echo $'        node_id=18'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:pfs)'
  echo $'        node_id=18'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:stack-up)'
  echo $'        node_id=19'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:su)'
  echo $'        node_id=19'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:stack-down)'
  echo $'        node_id=20'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:sd)'
  echo $'        node_id=20'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:stack-up-select)'
  echo $'        node_id=21'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:sus)'
  echo $'        node_id=21'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:stack-down-select)'
  echo $'        node_id=22'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:sds)'
  echo $'        node_id=22'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      0:completions)'
  echo $'        node_id=23'
  echo $'        node_word_count=1'
  echo $'        ;;'
  echo $'      *)'
  echo $'        break'
  echo $'        ;;'
  echo $'    esac'
  echo $'  done'
  echo $''
  echo $'  positional_index=$((${#non_options[@]} - node_word_count))'
  echo $'}'
  echo $''
  echo $'_git-cli_completions() {'
  echo $'  local cur=${COMP_WORDS[COMP_CWORD]}'
  echo $'  local prev='
  echo $'  if ((COMP_CWORD > 0)); then'
  echo $'    prev=${COMP_WORDS[$((COMP_CWORD - 1))]}'
  echo $'  fi'
  echo $''
  echo $'  local completed=()'
  echo $'  if ((COMP_CWORD > 1)); then'
  echo $'    completed=("${COMP_WORDS[@]:1:$((COMP_CWORD - 1))}")'
  echo $'  fi'
  echo $''
  echo $'  local non_options=()'
  echo $'  local completed_options=()'
  echo $'  local node_id='
  echo $'  local node_word_count=-1'
  echo $'  local positional_index=0'
  echo $'  local invalid_completion=0'
  echo $'  local flag_state=0'
  echo $'  _git-cli_completions_resolve_node'
  echo $''
  echo $'  local skip_next=0'
  echo $'  for word in "${completed[@]}"; do'
  echo $'    if ((skip_next)); then'
  echo $'      skip_next=0'
  echo $'      continue'
  echo $'    fi'
  echo $''
  echo $'    if [[ "${word:0:1}" == "-" ]]; then'
  echo $'      _git-cli_completions_node_flag_state "$node_id" "$word"'
  echo $'      flag_state=$?'
  echo $'      if (( flag_state == 1 )); then'
  echo $'        invalid_completion=1'
  echo $'        break'
  echo $'      fi'
  echo $''
  echo $'      completed_options+=("$word")'
  echo $'      if (( flag_state == 2 )); then'
  echo $'        skip_next=1'
  echo $'      fi'
  echo $'      continue'
  echo $'    fi'
  echo $''
  echo $'    non_options+=("$word")'
  echo $'    _git-cli_completions_resolve_node'
  echo $'  done'
  echo $''
  echo $'  COMPREPLY=()'
  echo $'  (( invalid_completion )) && return'
  echo $''
  echo $'  case "$node_id:$prev" in'
  echo $'  esac'
  echo $''
  echo $'  if [[ "${cur:0:1}" != "-" ]] && (( positional_index == 0 )); then'
  echo $'    case "$node_id" in'
  echo $'      0)'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "log l status s diff d checkout co add a commit c push ph pull pl fetch f rebase r reset rt restore re cherry-pick cp tag t clean update u commit-update-stack cus push-force-stack pfs stack-up su stack-down sd stack-up-select sus stack-down-select sds completions" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'    esac'
  echo $'  fi'
  echo $''
  echo $'  if [[ "${cur:0:1}" == "-" ]]; then'
  echo $'    case "$node_id" in'
  echo $'      0)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        _git-cli_completions_option_seen "--version" "-v" || words+=("--version" "-v")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      1)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        _git-cli_completions_option_seen "--graph" "-g" || words+=("--graph" "-g")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      2)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      3)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      4)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      5)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      6)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      7)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      8)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      9)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      10)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      11)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      12)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      13)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      14)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        _git-cli_completions_option_seen "--sync" "-s" || words+=("--sync" "-s")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      15)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      16)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        _git-cli_completions_option_seen "--stay" "-s" || words+=("--stay" "-s")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      17)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      18)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      19)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      20)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      21)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      22)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'      23)'
  echo $'        local words=()'
  echo $'        _git-cli_completions_option_seen "--help" "-h" || words+=("--help" "-h")'
  echo $'        while read -r; do COMPREPLY+=("$REPLY"); done < <(compgen -W "${words[*]}" -- "$cur")'
  echo $'        return'
  echo $'        ;;'
  echo $'    esac'
  echo $'  fi'
  echo $''
  echo $'  case "$node_id:$positional_index" in'
  echo $'    16:0)'
  echo $'      return'
  echo $'      ;;'
  echo $'  esac'
  echo $'} &&'
  echo $'  complete -F _git-cli_completions git-cli'
  echo $''
  echo $'# ex: filetype=sh'
}

# :command.command_functions
# :command.function
git_cli_log_command() {

  # src/log_command.sh

  flags=()

  if [[ ${args[--graph]} == 1 ]]; then
    flags+=(--graph --pretty=oneline --abbrev-commit --decorate)
  fi

  git log "${flags[@]}" "${other_args[@]}"

}

# :command.function
git_cli_status_command() {

  # src/status_command.sh

  git status "${other_args[@]}"

}

# :command.function
git_cli_diff_command() {

  # src/diff_command.sh

  git diff "${other_args[@]}"

}

# :command.function
git_cli_checkout_command() {

  # src/checkout_command.sh

  git checkout "${other_args[@]}"

}

# :command.function
git_cli_add_command() {

  # src/add_command.sh

  git add "${other_args[@]}"

}

# :command.function
git_cli_commit_command() {

  # src/commit_command.sh

  custom_args=()

  if [[ ${#other_args[@]} -eq 0 ]]; then

      commitType=$(gum choose "Amend" "Amend no edit" "Fixup" "Squash" --header "Select commit type" --limit 1)

      if [[ "$commitType" == "Amend" ]]; then
          custom_args+=(--amend)
      elif [[ "$commitType" == "Amend no edit" ]]; then
          custom_args+=(--amend --no-edit)
      elif [[ "$commitType" == "Fixup" ]]; then
          custom_args+=(--fixup HEAD)
          custom_args+=(-m $(gum input --width 50 --placeholder "Enter commit message"))
      elif [[ "$commitType" == "Squash" ]]; then
          custom_args+=(--squash HEAD)
          custom_args+=(-m $(gum input --width 50 --placeholder "Enter commit message"))
      fi

  fi

  git commit "${custom_args[@]}" "${other_args[@]}"

}

# :command.function
git_cli_push_command() {

  # src/push_command.sh

  git push "${other_args[@]}"

}

# :command.function
git_cli_pull_command() {

  # src/pull_command.sh

  git pull "${other_args[@]}"

}

# :command.function
git_cli_fetch_command() {

  # src/fetch_command.sh

  git fetch "${other_args[@]}"

}

# :command.function
git_cli_rebase_command() {

  # src/rebase_command.sh

  custom_args=()

  if [[ ${#other_args[@]} -eq 0 ]]; then
      targetOptions=(main Select)
      targetMode=$(gum choose "${targetOptions[@]}" --header "Select rebase target mode" --limit 1)
      target="main"

      if [[ "$targetMode" == "Select" ]]; then
          target=$(gum filter $(get_all_local_branches) --header "Select target" --limit 1)
      fi

      custom_args+=(-i --autosquash --onto $target)

      baseModeOptions=("Merge base" HEAD~ Select)
      selectBaseMode=$(gum choose "${baseModeOptions[@]}" --header "Select base mode" --limit 1)
      base="HEAD~"

      if [[ "$selectBaseMode" == "Select" ]]; then
          base=$(git log --oneline | gum filter --header "Select starting commit (this is non-inclusive)" --limit 1 | awk '{print $1}')
      fi

      if [[ "$selectBaseMode" == "Merge base" ]]; then
          base=$(git merge-base HEAD $target | tail -n 1)
      fi

      custom_args+=("$base")

      currentBranch=$(current_branch_name)

      custom_args+=($currentBranch)
  fi

  git rebase "${custom_args[@]}" "${other_args[@]}"

}

# :command.function
git_cli_reset_command() {

  # src/reset_command.sh

  git reset "${other_args[@]}"

}

# :command.function
git_cli_restore_command() {

  # src/restore_command.sh

  git restore "${other_args[@]}"

}

# :command.function
git_cli_cherry_pick_command() {

  # src/cherry_pick_command.sh

  git cherry-pick "${other_args[@]}"

}

# :command.function
git_cli_tag_command() {

  # src/tag_command.sh
  git tag "${other_args[@]}"

  if [[ ${args[--sync]} == 1 || -n ${args[--sync]} ]]; then
    git push origin --tags
  fi

}

# :command.function
git_cli_clean_command() {

  # src/clean_command.sh

  git fetch --prune --prune-tags

  allTargets=$(git for-each-ref --format '%(refname) %(upstream:track)' refs/heads | awk '$2 == "[gone]" {sub("refs/heads/", "", $1); print $1}')

  selectedTargets=$(gum choose ${allTargets[@]} --no-limit --header "Select none to delete all")
  : "${selectedTargets:=${allTargets[@]}}"

  git branch -D ${selectedTargets[@]}

  echo "${#selectedTargets[@]} local branches deleted"

}

# :command.function
git_cli_update_command() {

  # src/update_command.sh

  branch="${args[branch]}"

  if [[ -z ${args[branch]} ]]; then
    branches=($(git branch --format='%(refname:short)'))
    branch=$(gum choose "${branches[@]}" --header "Select a branch to update")
  fi

  git checkout "$branch"
  git fetch --prune
  git pull

  if [[ ${args[--stay]} == 0 || -z ${args[--stay]} ]]; then
    git checkout -
  fi

}

# :command.function
git_cli_commit_update_stack_command() {

  # src/commit_update_stack_command.sh
  custom_args=()

  if [[ ${#other_args[@]} -eq 0 ]]; then

      commitType=$(gum choose "Amend" "Amend no edit" "Fixup" "Squash" --header "Select commit type" --limit 1)

      if [[ "$commitType" == "Amend" ]]; then
          custom_args+=(--amend)
      elif [[ "$commitType" == "Amend no edit" ]]; then
          custom_args+=(--amend --no-edit)
      elif [[ "$commitType" == "Fixup" ]]; then
          custom_args+=(--fixup HEAD)
          custom_args+=(-m $(gum input --width 50 --placeholder "Enter commit message"))
      elif [[ "$commitType" == "Squash" ]]; then
          custom_args+=(--squash HEAD)
          custom_args+=(-m $(gum input --width 50 --placeholder "Enter commit message"))
      fi

  fi

  baseBranch=$(current_branch_name)

  if [[ -z "$baseBranch" ]]; then
      echo "Cannot update a stack from a detached HEAD"
      exit 1
  fi

  if [[ "$baseBranch" == "main" || "$baseBranch" == "master" ]]; then
      echo "Refusing to restack from $baseBranch; use \`g c\` to commit here"
      exit 1
  fi

  oldBase=$(current_hash)

  # Capture the stack layout before committing, while the old commit is still
  # the tip of the current branch.
  stackBranches=($(get_stack_branches_for_ref "$oldBase" | grep -v -x "$baseBranch"))
  terminalBranches=($(get_terminal_branches_for_ref "$oldBase" | grep -v -x "$baseBranch"))

  git commit "${custom_args[@]}" "${other_args[@]}" || exit $?

  newBase=$(current_branch_hash)

  if [[ "$newBase" == "$oldBase" ]]; then
      echo "No new commit was created; nothing to restack"
      exit 0
  fi

  if [[ ${#terminalBranches[@]} -eq 0 ]]; then
      echo "No branches are stacked on this commit; nothing to restack"
      exit 0
  fi

  # Map of rewritten commits (old hash -> new hash), seeded with the replaced
  # commit. Extended after every rebase so forked stacks rebase from their moved
  # fork point instead of duplicating the shared commits.
  mapOld=("$oldBase")
  mapNew=("$newBase")

  for terminal in "${terminalBranches[@]}"; do
      terminalTip=$(branch_hash "$terminal")

      # Rebase from the closest already-rewritten ancestor of this branch.
      rebaseFrom=""
      rebaseOnto=""
      bestDepth=-1
      for i in $(seq 0 $((${#mapOld[@]} - 1))); do
          if git merge-base --is-ancestor "${mapOld[$i]}" "$terminalTip"; then
              depth=$(git rev-list --count "${mapOld[$i]}")
              if [[ $depth -gt $bestDepth ]]; then
                  bestDepth=$depth
                  rebaseFrom="${mapOld[$i]}"
                  rebaseOnto="${mapNew[$i]}"
              fi
          fi
      done

      if [[ -z "$rebaseFrom" ]]; then
          echo "Skipping $terminal (no rewritten ancestor found)"
          continue
      fi

      preTips=()
      for branch in "${stackBranches[@]}"; do
          preTips+=("$(branch_hash "$branch")")
      done

      echo "Rebasing $terminal onto $(git rev-parse --short "$rebaseOnto")"
      if ! git rebase --update-refs --onto "$rebaseOnto" "$rebaseFrom" "$terminal"; then
          echo "Rebase of $terminal failed. Resolve the conflicts and finish with"
          echo "\`g r --continue\`, then restack any remaining branches manually."
          exit 1
      fi

      # Record every branch ref the rebase moved so later forks rebase from the
      # rewritten commits.
      for i in $(seq 0 $((${#stackBranches[@]} - 1))); do
          postTip=$(branch_hash "${stackBranches[$i]}")
          if [[ "$postTip" != "${preTips[$i]}" ]]; then
              mapOld+=("${preTips[$i]}")
              mapNew+=("$postTip")
          fi
      done
  done

  git checkout -q "$baseBranch"
  echo "Restacked ${#terminalBranches[@]} branch(es) onto $(git rev-parse --short "$newBase")"

}

# :command.function
git_cli_push_force_stack_command() {

  # src/push_force_stack_command.sh
  branches=($(get_stack_branches_for_ref HEAD))

  if [[ ${#branches[@]} -eq 0 ]]; then
      echo "No branches found at or above the current commit"
      exit 0
  fi

  pushed=0
  for branch in "${branches[@]}"; do
      if git show-ref --verify --quiet "refs/remotes/origin/$branch"; then
          echo "Force pushing $branch"
          git push --force origin "$branch" || exit $?
          pushed=$((pushed + 1))
      else
          echo "Skipping $branch (no origin/$branch to update)"
      fi
  done

  echo "Done force pushing stack ($pushed branch(es) pushed)"

}

# :command.function
git_cli_stack_up_command() {

  # src/stack_up_command.sh
  children=($(get_child_hashes_for_ref HEAD))

  if [[ ${#children[@]} -eq 0 ]]; then
      echo "Already at the top of the stack"
      exit 0
  fi

  target=$(choose_commit "Select the commit to move up to" "${children[@]}") || exit 1

  checkout_branch_or_ref "$target"

}

# :command.function
git_cli_stack_down_command() {

  # src/stack_down_command.sh
  parents=($(git rev-list --parents -n 1 HEAD))
  parents=("${parents[@]:1}")

  if [[ ${#parents[@]} -eq 0 ]]; then
      echo "Already at the bottom of the stack"
      exit 0
  fi

  target=$(choose_commit "Select the commit to move down to" "${parents[@]}") || exit 1

  checkout_branch_or_ref "$target"

}

# :command.function
git_cli_stack_up_select_command() {

  # src/stack_up_select_command.sh
  branches=($(get_downstream_branches_for_ref HEAD))

  if [[ ${#branches[@]} -eq 0 ]]; then
      echo "No branches downstream of the current commit"
      exit 0
  fi

  target=$(choose_branch "Select the downstream branch to check out" "${branches[@]}") || exit 1

  git checkout "$target"

}

# :command.function
git_cli_stack_down_select_command() {

  # src/stack_down_select_command.sh
  branches=($(get_upstream_branches_for_ref HEAD))

  if [[ ${#branches[@]} -eq 0 ]]; then
      echo "No branches between the current commit and the merge base with main"
      exit 0
  fi

  target=$(choose_branch "Select the upstream branch to check out" "${branches[@]}") || exit 1

  git checkout "$target"

}

# :command.function
git_cli_completions_command() {

  # src/completions_command.sh

  send_completions

}

# :command.parse_requirements
parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        git_cli_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    log | l)
      action="log"
      shift
      git_cli_log_parse_requirements "$@"
      shift $#
      ;;

    status | s)
      action="status"
      shift
      git_cli_status_parse_requirements "$@"
      shift $#
      ;;

    diff | d)
      action="diff"
      shift
      git_cli_diff_parse_requirements "$@"
      shift $#
      ;;

    checkout | co)
      action="checkout"
      shift
      git_cli_checkout_parse_requirements "$@"
      shift $#
      ;;

    add | a)
      action="add"
      shift
      git_cli_add_parse_requirements "$@"
      shift $#
      ;;

    commit | c)
      action="commit"
      shift
      git_cli_commit_parse_requirements "$@"
      shift $#
      ;;

    push | ph)
      action="push"
      shift
      git_cli_push_parse_requirements "$@"
      shift $#
      ;;

    pull | pl)
      action="pull"
      shift
      git_cli_pull_parse_requirements "$@"
      shift $#
      ;;

    fetch | f)
      action="fetch"
      shift
      git_cli_fetch_parse_requirements "$@"
      shift $#
      ;;

    rebase | r)
      action="rebase"
      shift
      git_cli_rebase_parse_requirements "$@"
      shift $#
      ;;

    reset | rt)
      action="reset"
      shift
      git_cli_reset_parse_requirements "$@"
      shift $#
      ;;

    restore | re)
      action="restore"
      shift
      git_cli_restore_parse_requirements "$@"
      shift $#
      ;;

    cherry-pick | cp)
      action="cherry-pick"
      shift
      git_cli_cherry_pick_parse_requirements "$@"
      shift $#
      ;;

    tag | t)
      action="tag"
      shift
      git_cli_tag_parse_requirements "$@"
      shift $#
      ;;

    clean)
      action="clean"
      shift
      git_cli_clean_parse_requirements "$@"
      shift $#
      ;;

    update | u)
      action="update"
      shift
      git_cli_update_parse_requirements "$@"
      shift $#
      ;;

    commit-update-stack | cus)
      action="commit-update-stack"
      shift
      git_cli_commit_update_stack_parse_requirements "$@"
      shift $#
      ;;

    push-force-stack | pfs)
      action="push-force-stack"
      shift
      git_cli_push_force_stack_parse_requirements "$@"
      shift $#
      ;;

    stack-up | su)
      action="stack-up"
      shift
      git_cli_stack_up_parse_requirements "$@"
      shift $#
      ;;

    stack-down | sd)
      action="stack-down"
      shift
      git_cli_stack_down_parse_requirements "$@"
      shift $#
      ;;

    stack-up-select | sus)
      action="stack-up-select"
      shift
      git_cli_stack_up_select_parse_requirements "$@"
      shift $#
      ;;

    stack-down-select | sds)
      action="stack-down-select"
      shift
      git_cli_stack_down_select_parse_requirements "$@"
      shift $#
      ;;

    completions)
      action="completions"
      shift
      git_cli_completions_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      git_cli_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_log_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_log_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="log"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --graph | -g)

        # :flag.case_no_arg
        args['--graph']=1
        shift
        ;;

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_status_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_status_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="status"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_diff_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_diff_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="diff"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_checkout_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_checkout_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="checkout"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_add_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_add_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="add"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_commit_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_commit_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="commit"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_push_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_push_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="push"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_pull_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_pull_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="pull"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_fetch_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_fetch_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="fetch"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_rebase_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_rebase_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="rebase"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_reset_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_reset_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="reset"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_restore_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_restore_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="restore"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_cherry_pick_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_cherry_pick_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="cherry-pick"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_tag_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_tag_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="tag"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --sync | -s)

        # :flag.case_no_arg
        args['--sync']=1
        shift
        ;;

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_clean_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_clean_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="clean"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_update_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_update_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="update"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --stay | -s)

        # :flag.case_no_arg
        args['--stay']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['branch']+x} ]]; then
          args['branch']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_commit_update_stack_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_commit_update_stack_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="commit-update-stack"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_push_force_stack_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_push_force_stack_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="push-force-stack"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_stack_up_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_stack_up_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="stack-up"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_stack_down_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_stack_down_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="stack-down"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_stack_up_select_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_stack_up_select_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="stack-up-select"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_stack_down_select_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_stack_down_select_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="stack-down-select"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
git_cli_completions_parse_requirements() {
  local key

  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        git_cli_completions_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="completions"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.initialize
initialize() {
  declare -g version="0.1.0"
  set -e

}

# :command.run
run() {
  # :command.globals
  declare -g long_usage=''
  declare -g -A args=()
  declare -g -a other_args=()
  declare -g -A deps=()
  declare -g -a env_var_names=()
  declare -g -a input=()

  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "log") git_cli_log_command ;;
    "status") git_cli_status_command ;;
    "diff") git_cli_diff_command ;;
    "checkout") git_cli_checkout_command ;;
    "add") git_cli_add_command ;;
    "commit") git_cli_commit_command ;;
    "push") git_cli_push_command ;;
    "pull") git_cli_pull_command ;;
    "fetch") git_cli_fetch_command ;;
    "rebase") git_cli_rebase_command ;;
    "reset") git_cli_reset_command ;;
    "restore") git_cli_restore_command ;;
    "cherry-pick") git_cli_cherry_pick_command ;;
    "tag") git_cli_tag_command ;;
    "clean") git_cli_clean_command ;;
    "update") git_cli_update_command ;;
    "commit-update-stack") git_cli_commit_update_stack_command ;;
    "push-force-stack") git_cli_push_force_stack_command ;;
    "stack-up") git_cli_stack_up_command ;;
    "stack-down") git_cli_stack_down_command ;;
    "stack-up-select") git_cli_stack_up_select_command ;;
    "stack-down-select") git_cli_stack_down_select_command ;;
    "completions") git_cli_completions_command ;;
  esac
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
  # :command.start
  command_line_args=("$@")
  initialize
  run "${command_line_args[@]}"
fi
