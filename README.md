# Summary

This is a set of helper bash functions and cli for dev workflow.

The bash clis are generated with bashly and use gum.

To use the bash scripts simply source the `init` file to your relevant shell source.

## Developing

When developing use `bashly generate --upgrade` to update the generted cli.
